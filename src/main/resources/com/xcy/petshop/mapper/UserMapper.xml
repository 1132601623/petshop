<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xcy.petshop.mapper">

    <select id="selectByEmail" parameterType="string" resultType="int">
        select count(1) from user where email = #{email}
    </select>

    <insert id="registerEmail" parameterType="user">
        insert into user (id , email, password) values (null, #{email}, #{password})
    </insert>

</mapper>